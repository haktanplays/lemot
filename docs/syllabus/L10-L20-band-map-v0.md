# L10–L20 Syllabus Band Map (v0) — Pre-Spec Planning

> **Purpose**: Plan the **L10–L20 band as a whole** *before* writing detailed lesson specs, to prevent **pacing overload**, **paywall-value leakage**, and **decision drift**. Follows `docs/learning-engine-v1.md`, `docs/syllabus/lesson-spec-template-v1.1.md`, `docs/syllabus/lesson-archetype-templates-v1.md`, `docs/syllabus/canonical-item-id-convention-v0.1.md`, `docs/syllabus/ai-generation-contract-v1.md`, the L1–L10 pilots, and the L1–L5 retrospective.
> **This is a map, NOT a set of lesson specs.** It authorizes **no** code, content, flag, or runtime change, and it does **not** lock the L11–L20 order — it surfaces the decisions that must be made first. Locked product canon wins on conflict. `v0` = first pass, expected to revise as L11+ specs are written.

> ## ⚠️ L18–L20 RECONCILED (2026-08-08) — read before using any L18/L19/L20 material below
>
> The post-L17 sequence is now ratified in **`docs/syllabus/L18-L20-sequence-decision-v1.md`**, with L18 settled in detail by **`docs/syllabus/L18-question-expansion-2-gate-review.md`**. Where this v0 map disagrees with either, **they win**. The rows and prose below are annotated in place; superseded material is marked, not deleted.
>
> | Lesson | Ratified job | Journey role | Active-new |
> |---|---|---|---|
> | **L18** | **Question Expansion 2 — a controlled `comment` doorway** | **doorway** | **1** (`adverb-comment`, planned) |
> | **L19** | Integration + weak-point repair + light second ASM recurrence | **integration** | **0** |
> | **L20** | Pre-Campfire capability checkpoint (proof, not teaching) | **milestone** | **0** |
>
> - **This map's "L18 = Futur proche stronger preview" is SUPERSEDED / RETIRED as a lesson.** Its *analysis* survives — futur proche stays reserved pre-Campfire — but a standalone 0-ownership preview lesson has **no legal journey role** (Doorway needs 1–2, Standard 1–4, and Content Bible §6.8.1 forbids a sixth role), and forward-seeding needs no lesson of its own (§6.8.4). The hook is re-homed as **FP-C**: one bounded recognition-only beat at the **end of L20**, 0 demands, 0 identities, no production target.
> - **Three claims in this map are factually wrong against shipped content** and are corrected in place below: the **L7 futur hook does not exist** (shipped L7 carries `Je vais à la maison.` only — no `je vais + inf.` in any status); **L17 carried no futur hook either** (zero occurrences of `je vais`); and the **`faire` carry-in is phantom** (there is no `verb-faire` — `faire` lives only inside the frozen `chunk-faire-une-pause`, and no infinitive slot is owned anywhere).
> - **L19's "weak-point repair" means the mastery/SRS recovery mechanism — NOT French conversational repair language.** The repair rail (`je ne comprends pas`, `vous pouvez répéter ?`, `c'est pas grave`) is orphaned, acknowledged as **RR-A**, and assigned to none of L18–L20.
> - **Charter status unchanged.** `CURRICULUM_CHARTER_v1.0.md` §5 still reads `L18+ | Open / provisional`. Founder direction is ratified; **Charter promotion remains pending lesson-spec completion.**
> - **L10–L17 rows are untouched by this reconciliation**, including §2's L1–L10 baseline table (which carries its own separately-recorded phantom-ownership debt, out of scope here).

> **⚠️ Paywall canon (settled for this band): Campfire / soft paywall around L24.** Active syllabus/product canon (`docs/MASTER_PIPELINE_v1.2.1.md` §2 + archetype #11) places the paywall/promise gate at **Campfire ~L24**. **Legacy "paywall after L14" references (`CLAUDE.md`, the 24-lesson bracket) are archive / superseded** unless a future operator decision explicitly reactivates them — the pipeline already lists *"Old L14/L15 paywall assumptions if contradicted by current canon"* as archive. **So this is NOT an unresolved product-direction decision for the L10–L20 band.** It is a **documentation/runtime alignment risk**: the repo may still carry old L14 references that should be cleaned up or clearly marked legacy in a later **docs-alignment step** (§12 / Open Items). **L10–L20 planning assumes Campfire ~L24**, making the band **entirely pre-Campfire / free-zone**; the commercial-depth review (§5) protects high-value systems before L24 **without** treating L14 as an active competing boundary.

---

## 1. Executive Summary

- **Recommended arc:** **Option C** (below) — a rhythm-respecting, commercial-depth-preserving sequence: **L10 Integration (shipped) → L11 Pouvoir-light → L12 Question-expansion-1 → L13 Integration → L14 `y`-light → L15 Devoir/Falloir-light → L16 Integration + A-Small-Moment seed → L17 Human-context/feelings → ~~L18 Futur-proche stronger-preview (not owned)~~ **L18 Question Expansion 2 (`comment` doorway)** → L19 Integration/weak-point repair → L20 Pre-Campfire checkpoint (milestone).**
- **Main pedagogical goal of the band:** turn the L1–L10 *survival + movement + small-action* learner into someone who can **ask for and offer help, ask richer questions, express obligation/ability, and handle a short real exchange** — while the **headline production engines stay previewed, not owned**.
- **Main commercial/paywall implication:** Campfire ~L24 is the gate (**settled** — legacy L14 is archive), so **L11–L20 are all free**. The band gives **real, usable ability** (help/permission, richer questions, obligation, feelings, `y`, the A Small Moment ritual) **without owning** the four headline engines (full futur proche, passé composé, full question formation, broad pouvoir + en/partitives) — those are the **reasons to cross Campfire**. The design goal is to **balance usefulness, trust, and preview value while reserving deeper ownership for the paid/promise zone** (§5). A strong free path does **not** require owning a headline engine.
- **Biggest risk:** ~~**the futur-proche ownership point and the resulting free-tier *tuning*** (§5) — i.e. *how much* the pre-Campfire band should do, and how strong the L18 futur preview should be, without spending the headline engines.~~ **RESOLVED (2026-08-08):** the tuning question is closed by retiring the preview lesson outright — the band spends **no** headline engine, and the futur hook shrinks to FP-C (one recognition-only beat, 0 demands, at the end of L20). **The band's remaining risk is the opposite one: under-delivery.** L18 owns a single adverb, so its failure mode is authoring `Comment ça va ?` as a second frozen chunk and calling it a doorway (gate review §1/§4.2). (The **paywall position itself is settled** — Campfire ~L24; legacy L14 is archive, so it is **not** a product-direction risk here, only a later docs-alignment cleanup.) Secondary risk, unchanged: **stacking control layers** (pouvoir + question formation + y + devoir) too densely, re-creating the L7–L9 acceleration the Integration Rhythm Rule was written to prevent.

---

## 2. L1–L10 Baseline (what the learner can do after L10)

| Capability | Owned form (examples) | From |
|---|---|---|
| Request politely | `je voudrais …`, `s'il vous plaît` | L1 |
| Greet / rescue / repair | `bonjour`, `merci`, `je ne comprends pas`, `pouvez-vous répéter ?`, `c'est pas grave` | L1/L2 |
| Identify self / state | `je suis …` (`fatigué`, `étudiant`) | L2 |
| Negate | `ne…pas` (`je ne suis pas …`, `je n'ai pas de …`) | L3 |
| Express need / human state | `j'ai besoin de …`, `j'ai faim` | L4 |
| Use objects/articles **lightly** | `un/une + noun`, `c'est le/la …` (recognition) | L5 |
| Integrate human context | recombine the foundation in non-café human scenes; `aide`, `comprendre` | L6 |
| Move / go somewhere | `je vais à + place`, `on va`, `je voudrais aller`, `je ne vais pas` | L7 |
| Ask controlled *where* | `où est … ?`, `c'est où ?`, `tu vas où ?` (spoken) | L8 |
| Do a small action / pause | `faire une pause`, `faire ça`, `je ne fais pas ça` | L9 |
| **Recombine** all of the above | a chained after-class human exchange | L10 |

**Already deferred (recognition / not owned):** futur proche (`je vais + inf.`), full question formation (`est-ce que`/inversion/most question words), `y`/`en`, broad faire (weather/sport/idioms/`se faire`/paradigm), present `vouloir` (`je veux`), `pouvoir` (frozen in `pouvez-vous répéter` only), location prepositions beyond `à/au/à la`, all past/future tense systems, partitives.

---

## 3. Proposed L10–L20 Table (Option C — the recommendation)

> L10 is **already shipped** — included as the anchor, not a new spec. Estimates are planning targets (engine doc §7), not validators. "Active/Supported-new" exclude recycled.

| # | Title | Primary archetype | Secondary | Owned target | Active-new | Supp-new | Major recycled engines | Deferred systems | Mon Lexique output | Practice/Daily hook | Commercial note | Risk |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| **L10** ✅ | After Class (Integration) | Review/Integration | Human-context | recombination skill | ~2 (meta) | ~3 | all L6–L9 | pouvoir, tense, Q-system | strengthen entries | mixed-engine recombination | free; previews pouvoir | low |
| **L11** | Pouvoir-light / Help & Permission | Architecture Verb (split-sense) | Chunk/Natural | `je peux` / `je ne peux pas` / `on peut … ?` / `vous pouvez m'aider ?` | ~7–9 | ~8–10 | faire (`faire ça`/`une pause`), aller, aide, ne…pas | broad pouvoir (probability, softening, `il se peut`), `est-ce que je peux` (recog) | new: pouvoir + peux chunks | can/ask-permission items | **free; light slice only — broad pouvoir reserved** | **med** |
| **L12** | Question expansion 1 (`est-ce que` yes/no wrapper) | Negation/Question (Question-control) | — | `est-ce que` **yes/no wrapper over already-owned clauses only** (no question words) | ~5–7 | ~7–9 | `où` (L8), tu/vous, movement, pouvoir (L11) | **question words** (`comment`/`combien`/`pourquoi`/`quand`/`qui`/`que` → Question Expansion 2), inversion, `qu'est-ce que`, full Q-system | new: `est-ce que` wrapper frame (no Q-words) | ask-a-yes/no-question items | **free; controlled — question-word system + full Q-system reserved** | **high** |
| **L13** | Integration / Can-do + Asking | Review/Integration | Human-context | recombine L11–L12 + movement | ~0–2 (meta) | ~3 | pouvoir, questions, aller, faire | (all deferred hold) | strengthen entries | can-do + ask recombination | free; integration beat | low |
| **L14** | `y` as place pronoun (light doorway) | Pronoun/Particle | Chunk/Natural | `j'y vais` / `on y va` **near-chunks, recognition-first** (place `y` only — not a broad replacement rule) | ~4–6 | ~6–8 | aller, où, places | `en` + `y`/`en` contrast, object/indirect pronouns + stacking, `y`=other senses, `il y a` (separate), broad `à+place` replacement, `y`+pouvoir/`est-ce que` | new: `y` (place sense, chunk-first) | `j'y vais`/`on y va` items | free; narrow doorway | **med** |
| **L15** | Devoir / Falloir light (obligation) | Architecture Verb (split-sense) | Chunk/Natural | **asymmetric**: `il faut + inf.` **primary** (invariable) + `je dois + inf.` **supported** (one form) | ~6–8 | ~8–10 | pouvoir (contrast), faire, aller, infinitive frames | full `devoir` paradigm, `il faut que + subj.`, conditionnel/advice (`devrais`/`faudrait`), `il me faut`/`il faut`+noun, `devoir`=owe, `y`+devoir, past/future | new: `il faut`/`je dois` chunks | must/have-to items | free; light slice only | **high** |
| **L16** *(RESOLVED — Step 22A/22B)* | Integration + A Small Moment seed | Review/Integration | Human-context/Reading | recombine **L11–L15** + **`model-answer-only` A Small Moment seed** (present-only, known-items-only ≤2–3 line read + 1 scaffolded response) | ~1–3 (meta) | ~3–4 | L11–L15 engines | live AI/free chat/personalization/deep ASM, past-future-in-reading, object-pronoun production, y+modal chains *(holds)* | strengthen + first reading entry | A Small Moment micro-read | free; **seed shape only — deep AI-driven ASM paid-zone**; L19 may recur lightly (L16 does not launch the full feature) | med |
| **L17** *(RESOLVED — Step 23A/23B)* | Human Context / Feelings **Light** | Thematic Vocabulary/Context *(integration restraint)* | Chunk/Natural | **social check-in `ça va ?`/`ça va`/`ça ne va pas` + 1 capped positive adjective (`content`/`contente`) + recycled states** | **~3–5 (NOT 7–9)** | ~4–6 | être-state, avoir-state, il faut/je dois | **advice/conditionnel (`tu devrais`/`il faudrait`) demoted → paid-zone**, `se sentir`/`je me sens`, broad emotion lexis, opinion verbs, past/future feeling narrative, futur proche, deep emotional/social AI | new: `ça va` chunk-set + `content`/`contente` | check-in + feeling items | free; **feelings-light, recycle-dominant** | med |
| ~~**L18**~~ *(SUPERSEDED — retired 2026-08-08)* | ~~Futur proche **stronger preview** (NOT owned)~~ | ~~Tense/Aspect/Mood Doorway~~ | ~~Chunk/Natural~~ | ~~recognition→supported recognition of `je vais + inf.`; **0 active ownership**~~ | ~~~3–5 (mostly recog)~~ | ~~~6–8~~ | ~~aller, faire, infinitive~~ *(**phantom** — no `verb-faire` exists and no infinitive slot is owned)* | ~~**full futur proche ownership** (post-Campfire)~~ | — | — | ~~free; promise hook~~ | — |
| **L18** *(RATIFIED — sequence decision + gate review)* | **Question Expansion 2 — controlled `comment` doorway** | **prose #3 Negation/Question/Social Choice** → schema **`chunk-natural-speech`** (L8/L12 precedent; **prose #3 has no enum member**) | open — compact spec decides | **`comment`, manner/state sense only**, into two already-owned frames: `C'est comment ?` (L8 end-placement) and `Comment ça va ?` (front-placed with L17's frozen chunk) | **1** — `adverb-comment` *(planned; **journeyRole `doorway`**, band 1–2, PASS)* | small — the answers are all L17-recycled | L8 `où` + end-placement · L12 `est-ce que` · L17 `ça va` / `ça ne va pas` / `je suis fatigué(e)` / `je suis content(e)` · `chunk-faire-une-pause` **as a package only** | every other Q-word · **`comment` + `est-ce que`** · inversion · `qu'est-ce que` · embedded Qs · the **`Comment ?` repair** sense (RR-A) · the procedural sense · **futur proche** | new: `comment` (manner/state) | ask-how + answer-about-self | **free; ONE Q-word — the band's 1–2 allowance is deliberately underspent** | **med** — the risk is **thinness**, not leakage |
| **L19** *(RATIFIED)* | Integration / **weak-point repair (mastery mechanism, NOT repair language)** / light second A Small Moment recurrence | Review/Integration | Reading / Human-context *(compact spec picks a real enum member)* | recombine L11–L18 + weak-point recovery | **0** *(**journeyRole `integration`**, plain band, **no exception**)* | ~3 | all band engines | (holds) · **repair language stays orphaned — RR-A** | mastery events, weak-point recovery | weak-point repair pool | free; consolidation | low |
| **L20** *(RATIFIED)* | Pre-Campfire checkpoint — **proof, not teaching** | Milestone/Transition → schema **`summit-milestone`** | Human-context *(compact spec picks a real enum member)* | exit proof: **carry one small French moment end to end** (greet → ask → answer → need → obligation/permission/movement → close) with minimal or no supplied pieces | **0** *(**journeyRole `milestone`**, band 0–3, PASS)* | ~2–3 | everything owned L1–L19 | (holds) | reflection view | curated proof set | **free; the on-ramp to Campfire @ L24** | med — **first shipped Milestone**; empirically validates the contract (Content Bible §6.8.5) |

> **Band totals (Option C, as reconciled):** ~5 engine/control lessons (L11, L12, L14, L15, L17) + **1 controlled Q-word doorway (L18)** *(was: ~1 preview)* + 4 integration/milestone beats (L13, L16, L19, L20). Headline engines (full futur proche, passé composé, full Q-system, broad pouvoir, en/partitives) **owned: 0** in this band — **unchanged, and now with no preview lesson spending any of them.**
>
> **L18 forward-hook note:** the only futur-proche exposure left in the band is **FP-C** — one recognition-only beat at the **end of L20**, 0 demands, 0 identities, no production target. It must **not** be described as landing a preview begun at L7 or L17: **neither shipped lesson carries one.**

---

## 4. Candidate L10–L20 Arcs (≥3 options evaluated)

### Option A — Safer / rhythm-first (as proposed in the brief)
L10 Integration ✅ · L11 Pouvoir-light · **L12 Integration/Can-do+Help** · L13 Question-exp-1 · L14 `y` · L15 Review/ASM-seed · L16 Devoir/Falloir · L17 Human-context · L18 Futur-preview · L19 Integration · L20 Gate.
- **Strengths:** maximally safe pacing; every new engine quickly consolidated; near-zero overload risk.
- **Risks:** **over-integration** — L10 + L12 + L15 + L19 + L20 = **5 of 11 lessons are integration/review/gate**, with only *one* new engine (pouvoir) between L10 and L12. High "**too little novelty**" risk; the free band may feel slow/repetitive.
- **Commercial impact:** safe (nothing leaked), but a slow free band can *hurt* conversion (learner stalls before seeing enough value to want Campfire).
- **Pacing impact:** under the rhythm rule's ceiling, but arguably *below the floor* — integration is meant to follow ~3 new engines, not interleave after every one.
- **Recommendation:** **second choice.** Good guardrails, but too cautious; merge its caution into C.

### Option B — Momentum / engine-heavy (as proposed)
L10 Integration ✅ · L11 Pouvoir-light · **L12 Question-exp · L13 `y` · L14 Faire-continuation** · L15 Review · L16 Devoir/Falloir · **L17 En/quantity** · L18 Futur-deeper-preview · L19 ASM · L20 Gate.
- **Strengths:** strong forward momentum; lots of new capability fast.
- **Risks:** **violates the Integration Rhythm Rule** — **L11–L14 is four consecutive new-engine/control lessons** (pouvoir → question → y → faire) before any integration at L15. Re-creates exactly the L7–L9 acceleration the rule was written to stop. Also **L17 En/quantity + partitive** is advanced and **leaks paid-zone value**; **L18 "deeper" futur preview** risks tipping into ownership.
- **Commercial impact:** **negative** — opens too many systems pre-Campfire (en/partitive, deep futur), eroding the promise zone.
- **Pacing impact:** **fails the rule.** Overload-prone.
- **Recommendation:** **reject as-is.** Only viable if heavily re-cut (insert integration after L12, drop en/partitive, keep futur a light preview) — at which point it becomes C.

### Option C — Commercial-depth preserving + rhythm-respecting (RECOMMENDED)
L10 Integration ✅ · L11 Pouvoir-light · L12 Question-exp-1 · **L13 Integration** · L14 `y`-light · L15 Devoir/Falloir-light · **L16 Integration+ASM seed** · L17 Human-context/feelings · ~~L18 Futur stronger-preview~~ **L18 Question Expansion 2 (`comment`)** · **L19 Integration/repair** · L20 Pre-Campfire checkpoint.
- **Strengths:** **2 new engines → 1 integration** cadence (L11–L12→L13; L14–L15→L16; L17–L18→L19) — healthier than A's over-integration and safe vs B's overload. Headline engines all previewed, none owned. Introduces the **A Small Moment** ritual at L16 as the integration vehicle (novelty without a new grammar system). Ends on a capability milestone (L20) that on-ramps Campfire. *(The cadence survives the L18 substitution intact: L17–L18 is still 2 consecutive new-capability lessons before L19's beat.)*
- **Risks:** **L12 (question expansion) and L15 (devoir/falloir) are the two genuine overload candidates** (§7); ~~the **L18 futur preview must be policed hard** to not become ownership~~ **— resolved by retiring the preview lesson (see the banner). L18's replacement carries the opposite risk: one adverb is a thin doorway, and the guard is the two-host-frame requirement, not a leak trap.**
- **Commercial impact:** **best** — strong free usefulness (help/permission, richer questions, obligation, feelings, `y`) while the four headline engines stay for Campfire+.
- **Pacing impact:** respects the rule with margin; never >2 consecutive new before an integration beat.
- **Recommendation:** **adopt as the working arc** — but treat L12+ as provisional pending §10 gates (especially the futur-proche ownership point + free-tier tuning; the paywall position itself is settled).

---

## 5. Paywall / Commercial Depth Review

> **Canon (settled): Campfire ~L24 = paywall/promise gate ⇒ L10–L20 are all pre-Campfire / free.** Legacy L14 is archive (see header) — **not** an active competing boundary. The review below protects high-value systems before L24 on that basis.

| System | Classification | Rationale |
|---|---|---|
| **pouvoir** | **early-free owned (light slice)** at L11 | help/permission is high-utility and socially essential; own `je peux`/`on peut`/`vous pouvez m'aider`. **Broad pouvoir** (probability, polite softening of every request, `il se peut`) → **paid-zone deepened**. |
| **devoir / falloir** | **early-free owned (light slice)** at L15 | `je dois + inf.` / `il faut + inf.` are core A1; light ownership is fine. `il faut que + subjonctif` → **post-paywall**. |
| **futur proche** | ~~**early-free PREVIEW only** (L7 hook → L18 stronger preview)~~ → **early-free: ONE recognition-only beat at the end of L20 (FP-C)**; **paid-zone owned** | the "going to *do* X" engine is the single strongest free-tier temptation; **owning it pre-Campfire spends the promise**. Own at/after Campfire approach. **Correction (2026-08-08): the "L7 hook" does not exist** — shipped L7 carries `Je vais à la maison.` only, with no `je vais + inf.` in any status, and shipped L17 contains no `je vais` at all. **No shipped lesson has previewed futur proche.** FP-C is the first, and it is 0 demands / 0 identities / no production target. |
| **full question formation** | **early-free: controlled slice only** (L8 `où` end-placement; L12 `est-ce que`; **L18 exactly ONE Q-word, `comment`**); **paid-zone: full system** (inversion, remaining Q-words, `qu'est-ce que`, `Q-word + est-ce que`, embedded questions) | controlled questions are needed to function; the *full* system is deep and reservable. **The "1–2 Q-words" allowance was written against L12 and went unspent there; L18 takes exactly one of it and deliberately leaves the second.** |
| **`y`** | **early-free owned (place sense only)** at L14 | narrow, high-frequency, pairs with movement; other `y` senses + `en` deferred. |
| **`en`** | **paid-zone owned** (NOT in L10–L20 under Option C) | quantity/partitive pronoun is advanced; pairing it with partitives pre-Campfire over-loads and leaks value. |
| **partitives (du/de la/des)** | **early-free: recognition only**; **paid-zone owned** | food/quantity system; keep `pas de` (supported, L5) but defer the partitive *system*. |
| **past tense / passé composé** | **paid-zone owned** (post-Campfire) | the second headline engine; do not open in L10–L20 at all (not even strong preview before L20). |
| **A Small Moment** | **early-free owned (seeded L16, recurs)** | the signature reading-response ritual is a *retention/usefulness* feature, not a grammar engine — safe and motivating to give free; **deeper AI-driven A Small Moment** → paid-zone deepened. |
| **Practice Pool Challenge** | **early-free: Build+Stretch throughout; Challenge from ~L13** | Challenge (free production) should expand as owned engines accumulate; full open-ended Challenge → paid-zone deepened. |
| **Mon Lexique depth** | **early-free: learner notebook (planning only; dev-apk out of scope)**; **paid-zone: deeper AI/Word-Graph** | per DEV_APK canon + engine §14 tiering. |
| **AI Natural Reveal depth** | **early-free: model-answer-only (L0–L20)**; **paid-zone: live AI evaluation** | L0–L10 are all `model-answer-only`; keep AI evaluation for the paid tier (AI contract §3 `feedback_mode`). |

**Objective verdict (both directions):**
- **Giving away too much?** **No, provided the four headline engines (full futur proche, passé composé, full question formation, en+partitives) stay reserved.** Option C does this; **Option B gives away too much** (en/partitive, deep futur).
- **Withholding too much / free path weak?** **No — the free band is strong on its own terms.** Help/permission (pouvoir-light, scoped narrowly as help/permission rather than full pouvoir), richer questions, obligation (devoir-light), feelings, `y`, and the A Small Moment ritual give **real, usable conversational ability** — usefulness + trust + preview value — **without** needing to own a headline engine. ~~The genuine question is a **tuning** one (not a weakness): **how strong should the L18 futur-proche preview be** — light recognition vs near-ownership "supported"? A near-ownership preview gives the free tier one big-feeling capability just before Campfire while **still reserving full ownership for the paid/promise zone**.~~ **RESOLVED (2026-08-08): the tuning question is closed by removing the lever.** There is no futur-proche preview lesson to tune; FP-C is fixed at the minimum (recognition-only, 0 demands, end of L20). Perceived free-tier strength now rests on **A Small Moment + feelings + the ask/answer capability L17→L18 completes** — a learner who can greet, ask how someone is, answer honestly, ask where and how, request, and state an obligation. **The promise zone is spent by exactly nothing.** (Moving the paywall is **not** a lever here — Campfire ~L24 is settled.)

---

## 6. Pacing Review (Integration Rhythm Rule: ≤ ~3 consecutive new-engine/control lessons)

**Integration/review beats in Option C:** L10 ✅, **L13**, **L16**, **L19**, L20 (milestone). New-engine/control runs between beats: L11–L12 (2), L14–L15 (2), L17–L18 (2). **Never exceeds 3.** ✅

Explicit flags:
- **Too many architecture verbs in a row?** L11 pouvoir + L15 devoir/falloir are the only two architecture verbs in the band, **well separated** (L13 integration + L14 `y` between them). ✅ Do **not** add a third architecture verb before L20.
- **Too many question layers?** **Reconciled (2026-08-08): two, and that is the ratified shape.** L12 owns the `est-ce que` **mechanism**; L18 owns **one question word** (`comment`) into frames the learner **already** owns — it opens **no new question mechanism**, which is why it is not the "second question layer" this row warned against (that warning was aimed at Option B stacking a second *mechanism*). The full Q-system — inversion, the remaining Q-words, `qu'est-ce que`, `Q-word + est-ce que`, embedded questions — stays reserved. ✅ **Do not** add a third.
- **Too many tense/aspect/mood doorways?** ~~Only **one**, and it's a **preview** (L18), not an owned doorway.~~ **Reconciled: ZERO.** The preview lesson is retired; only FP-C survives, as a recognition-only beat at the end of L20. ✅ Past tense stays out entirely.
- **Too much vocabulary expansion?** Only **L17** (feelings) is vocabulary-forward, and it must stay *in frames* (thematic archetype QA risk). ✅ Watch for an emotional-lexis dump.
- **Too much café/travel context?** The band must **continue the L6→L10 café reduction**; after-class / help / home / class scenes, not café orders or travel itineraries. Flag: `y`-light (L14) tempts travel framing — **keep human, not touristic**. *(The futur-preview travel temptation is gone with the lesson; L18's `comment` is human-context by construction.)*
- **Too little novelty after integration lessons?** **The standing risk** (L6/L10 both flagged it). ~~With 4 integration/milestone beats in the band, L13/L16/L19 must each carry a **forward preview hook** (per the archetype #10 rule) — L13→`y`, L16→devoir/ASM, L19→futur/Campfire — to avoid feeling like repeated quizzes.~~ **Narrowed (2026-08-08), consistent with the founder decision already applied to L16:** a forward preview hook is **permitted, not required**. Shipped L16 carries **none** and answers the risk through its A Small Moment seed instead. **L19's differentiator is likewise the second ASM recurrence plus weak-point recovery — not a futur hook**, which now lives at the end of L20 as FP-C.

---

## 7. High-Risk Lesson Candidates

| Lesson | Why risky | Likely leak / deferred-system risk | Mitigation | Split? |
|---|---|---|---|---|
| **L12 Question expansion** | `est-ce que` + question words is a large system; easy to over-open | full Q-system leak (inversion, all Q-words, embedded Qs) | own **only** the `est-ce que` **yes/no wrapper** over owned clauses; **defer all question words** (`comment`/`combien`/`pourquoi`/`quand`/`qui`/`que`) to a later **Question Expansion 2**; keep inversion + `qu'est-ce que` recognition; reuse L8 `où` | **yes (planned)** — L12 = `est-ce que` wrapper only; question words → later Question Expansion 2 |
| **L15 Devoir/Falloir** | two obligation verbs (`devoir` + impersonal `il faut`) at once; modal density right after pouvoir(L11) | broad devoir + `il faut que + subjonctif` leak; pouvoir/devoir confusion | own **one** light slice each (`je dois + inf.`, `il faut + inf.`); subjonctif deferred; lean on L11 pouvoir contrast | **possible** — could own only `il faut + inf.` and defer `devoir` |
| ~~**L18 Futur proche preview**~~ *(RETIRED)* | ~~strongest temptation to tip preview → ownership; spends the promise~~ | ~~**futur proche ownership leak** (the #1 commercial risk)~~ | **Resolved by retirement, not by policing.** The lesson is gone; FP-C caps the exposure at one recognition-only beat, 0 demands, 0 identities, end of L20. | — |
| **L18 Question Expansion 2 (`comment`)** *(replacement)* | a question word demands an **answer** the learner may not own — a worse offender than L12's wrapper, which only operated on owned sentences | **(a) `Comment est-ce que ça va ?`** — both halves owned, combination correct French, L12 held `Q-word + est-ce que` at recognition: **the #1 leak**; **(b) unowned answers** (`Je vais bien.` / `Pas mal.`); **(c)** inversion (`Comment allez-vous ?`) as the model's first instinct | own **one** Q-word in **one** sense (manner/state); answers restricted to L17's shipped set; reuse `trap:inversion-too-early` (L8), `trap:question-word-dump` (L12), `trap:qu-est-ce-que-overload` (L9) | no |
| **L18 thinness** *(the authoring risk, distinct from the leak)* | one adverb is a very small doorway | `Comment ça va ?` authored as a **second frozen social chunk** → L18 has taught a greeting variant wearing a `doorway` label | **binding:** demonstrate the slot in **two structurally distinct host frames** — `C'est comment ?` (L8 end-placement) **and** `Comment ça va ?`. Gate review §4.2. | no |
| **L17 Human context/feelings** | thematic → vocabulary-dump risk | emotional-lexis dump; opinion-verb leak (`je pense que`) | feelings **in owned frames** (`je suis …`, `j'ai besoin de …`); cap new nouns ~6–8; no opinion verbs | no |
| **L14 `y`** | pronouns are abstract; "production before recognition" risk (archetype #6) | `en` leak; `y`=other senses; object-pronoun leak | own **place `y` only** (`j'y vais`/`on y va`); recognition-first; `en` deferred | no |
| **L11 Pouvoir** | first new engine after integration; modal + the band's anchor | broad pouvoir (probability/softening) leak; present `vouloir` creep | split-sense light slice (per archetype #2); `est-ce que je peux` recognition | no |

---

## 8. AI Generation Risk Review (ties to `ai-generation-contract-v1.md`)

| Leak risk | Where it bites in L10–L20 | Contract anchor / mitigation |
|---|---|---|
| **Futur proche leak** | ~~L18 preview (and any aller reuse)~~ → **any `aller` reuse, and `ça va` → `ça va aller` → `je vais + inf.`** | §6 tense guardrails + the L7 row: `je vais + inf.` **never produced**. `chunk-ca-va`'s registry meaning already carries the guard (*"NOT productive aller and NOT futur proche"*) and `weakPointTags: ["aller-future"]` — **keep it through L18–L20**. **There is no L18 preview row to lean on: the preview lesson is retired**, and neither shipped L7 nor shipped L17 previewed the form. |
| **Question-word overreach** *(new — L18)* | `Comment est-ce que ça va ?` · inversion · a second Q-word · `qu'est-ce que` · unowned answers (`Je vais bien.`) | §8 frame guardrail: only lesson-listed frames, blocked **even when correct**; answers restricted to L17's shipped set; reuse `trap:inversion-too-early` / `trap:question-word-dump` / `trap:qu-est-ce-que-overload` |
| **Past tense leak** | any "what did you do" prompt; L16/L19 reading | §6: passé composé/imparfait blocked; reading micro-paragraphs use **present only** |
| **Vocabulary dump** | L17 feelings, L12 Q-words | §7: 0–2 low-risk nouns max, **in frames**; no thematic word lists |
| **Question-system leak** | L12 (`est-ce que`/Q-words), and after | §8 frame guardrail: only lesson-listed Q-frames; inversion + extra Q-words blocked even if correct |
| **Pronoun `y`/`en` leak** | L14 `y` | own place `y` only; **`en` blocked** as production; object pronouns blocked |
| **Broad faire/pouvoir leak** | L11 pouvoir, any faire reuse | L9/L11 notes: small-action faire only; pouvoir help/permission slice only; broad senses blocked |
| **Generic chatbot drift** | L16/L17 human scenes, Say-It-Your-Way | §11: scoped production-and-mirror, not open dialogue |
| **Mon Lexique orphan items** | L12/L14/L15/L17 new IDs | §14: every produced word maps to a canonical entry; new IDs declared per spec |
| **Over-helpful Natural Reveal** | every lesson (L0–L20 are `model-answer-only`) | §10: one "why", passive mirror; **no live AI eval pre-paywall**; don't teach deferred forms on the spot |

> **New contract rows needed** (when specs are written): L11–L20 each want an §15 allowed/blocked row, exactly as L6–L10 have. The band map does **not** add them (that's per-spec).

---

## 9. Mon Lexique / Canonical-ID Readiness

| Area | Stress in L10–L20 | Note (no runtime change) |
|---|---|---|
| Canonical ID convention | new lexemes: `word:pouvoir`, `word:devoir`, `word:y` (place), feelings nouns; frames: `frame:je-peux-plus-infinitive`, `frame:est-ce-que-plus-clause`, `frame:il-faut-plus-infinitive` | convention handles these; declare per-spec |
| **Homograph / sense-suffix** | **`word:y`** (pronoun) — guard against collision with any future use; **`peux`/`peut`** are not homographs but `peut`≈`peut-être` later; **`où`/`ou`** already handled | apply the L8 sense-suffix rule; flag `y` and any `peut`/`peut-être` split when reached |
| Chunk→word promotion | `aider` (recog in L10 `vous pouvez m'aider`) → promote at L11; `pouvez-vous` frozen → `pouvoir` opened at L11 | the L9/L10 promotion note covers this; status-mark explicitly |
| Frame IDs | several new (`je peux + inf.`, `est-ce que + clause`, `il faut + inf.`, `j'y vais`) | standard `frame:` prefix |
| Trap IDs | `trap:pouvoir-probability-too-early`, `trap:inversion-too-early` (reuse), `trap:en-leak`, `trap:past-tense-leak`, `trap:futur-proche-ownership` | per-spec |
| Word Graph future edges | pouvoir↔faire↔aller modal/verb-chain edges; `y`↔place nodes | placeholder only (engine §15) |
| Mon Lexique learner-facing simplicity | risk of exposing modal/tense jargon | keep surface simple (meaning/examples/where-met); IDs internal |

**Verdict:** the band does **not** break the ID convention; the only new ID-design question is the **`y` pronoun homograph guard** and the **`peut`/`peut-être` split** if probability is ever previewed. No runtime ID change now.

---

## 10. Decision Gates (must be answered before writing L11–L20 specs)

1. **Paywall/promise position — SETTLED: Campfire ~L24** (legacy L14 = archive/superseded). **Not a product-direction decision for this band.** Residual action is a later **docs-alignment pass** to clean up / mark-legacy any stale L14 references in the repo (do not edit unprompted). With this settled, the genuine top decision is **gate 4** (futur-proche ownership) + the §5 free-tier *tuning*.
2. **L11 pouvoir-light exact owned scope** — `je peux` + `on peut … ?` + `vous pouvez m'aider ?` only? Is `je ne peux pas` active or supported? Is `est-ce que je peux …?` recognition?
3. **L12 = question expansion 1 (RESOLVED — `est-ce que` yes/no wrapper only)** — a question layer comes at L12 (Option C), owning **only** the `est-ce que` yes/no wrapper over already-owned clauses. **Question-word expansion** (`comment`/`combien`/`pourquoi`/`quand`/`qui`/`que`) is **deferred to a later "Question Expansion 2"** lesson (per the L12 gate review + compact spec). L13 integration follows.
4. **Futur proche — RESOLVED (2026-08-08).** It becomes owned **at/after the Campfire approach, not in L10–L20**. ~~L18 = preview only.~~ **The standalone preview lesson is RETIRED** — a 0-ownership teaching lesson has no legal journey role (Content Bible §6.8.1; no sixth role may be added), and forward-seeding needs no lesson of its own (§6.8.4). Exposure is now **FP-C**: one recognition-only beat at the **end of L20**, 0 demands, 0 identities, no production target. The Charter's *"futur-proche ownership"* open item (§13) is **narrowed, not closed** — no pre-L24 ownership may be inferred.
5. **When does full question formation begin? — RESOLVED (2026-08-08).** Controlled `est-ce que` at **L12** (mechanism); **exactly one question word, `comment`, at L18** (manner/state sense only, into already-owned frames, no new mechanism). Inversion, the remaining Q-words, `qu'est-ce que`, `Q-word + est-ce que`, and embedded questions stay **post-Campfire**.
6. **When does `y` appear?** (Recommend L14, place sense only.) **When does `en` appear?** (Recommend: not in this band — paid-zone.)
7. **Devoir/Falloir — RESOLVED (Step 21A/21B): L15, asymmetric light.** `il faut + inf.` **primary** (impersonal, invariable → no paradigm) + `je dois + inf.` **supported** (one person-form). The full `devoir` paradigm, **`il faut que + subjonctif`**, conditionnel/advice (`je devrais`/`il faudrait`), `il me faut`/`il faut + noun`, `devoir`=owe, `y`+devoir, and past/future are **deferred**. Smoke fallback: `il faut + inf.` only. **L16 remains Integration + A Small Moment seed.**
8. **A Small Moment — RESOLVED (Step 22A/22B): first owned at L16 as a bounded seed.** L16 owns A Small Moment **only** as a **`model-answer-only` reading-response seed** — present-only, known-items-only, **≤2–3 line read + one short scaffolded response + deterministic reveal**. **Live AI evaluation, free chat, personalization, deeper response intelligence, and the deep/paid-depth A Small Moment remain deferred (paid-zone).** **L19 may recur it lightly; L16 does not launch the full feature.** Fallback if live AI is ever required: pure integration.
9. **When does Practice Pool Challenge expand?** (Recommend from ~L13 as owned engines accumulate.)
10. **L20 — RESOLVED (2026-08-08) for the L18–L20 slice.** L20 = **`journeyRole: milestone`, 0 acquisition demands**, a pre-Campfire **capability proof, not teaching**: the learner carries one small French moment end to end (greet → ask → answer → need → obligation/permission/movement → close) with minimal or no supplied pieces. It tests **integrated capability, not cumulative vocabulary recall**, and would be the **first shipped Milestone lesson** (Content Bible §6.8.5 records Milestone as empirically unvalidated), so authoring it also validates the contract. **L21–L24 remain open** — the Campfire @ L24 gate is Product-Brain-owned (Charter §13, PRJ-036) and is not confirmed here.
11. ~~**Free-tier strength tuning (from §5):** how strong should the **L18 futur-proche preview** be — light recognition vs near-ownership "supported"?~~ **RESOLVED (2026-08-08) by removing the lever** — there is no preview lesson to tune, and FP-C is fixed at the minimum. Free-tier strength rests on the ask/answer capability L17→L18 completes, plus A Small Moment and feelings. **The promise zone is spent by nothing.** (Moving the paywall was never on the table — Campfire ~L24 is settled.)

---

## 11. Recommended Next Detailed Spec

**Recommendation: write L11 only, then audit/re-map before L12.**

- **Why L11 only (not L11–L12 pair, not a batch):** L11 (pouvoir-light) is the **one lesson whose placement and rough scope are agreed and stable** — it's a free, light, high-utility slice that L10 already seeded. Everything from **L12 onward depends on §10 gates 3–5 + 11** (when/how question formation, `y`, and the futur-proche preview open, and the free-tier tuning) — which are **unresolved engine-ownership/pacing decisions** (the paywall position itself is settled at Campfire ~L24; it's the *engine-ownership timing within* the free band that's open). Writing L12–L20 now would bake in assumptions likely to be re-cut → exactly the **decision drift** this map exists to prevent.
- **Why not batch:** batching L11–L20 multiplies drift risk and would spend a lot of effort on specs sitting on unresolved engine-ownership/tuning decisions (§10).
- **Sequencing:** **write L11 → confirm the §10 gates (especially the paywall) → then decide L12 (and whether C's L12=question-expansion holds) → continue one lesson (or at most one pair) at a time.** A **Codex/docs consistency audit** is *not* required before L11 (the L1–L10 docs are internally consistent and L11 reuses established guardrails), but **is worth running once around L13–L16** when several new IDs/contract rows have accumulated.

---

## 12. Final Verdict

> **Status update (2026-08-08).** This §12 verdict was written at v0, before L11–L17 shipped. **L11–L17 are shipped**; **L18 has a ratified sequence position and a gate review** and is ready for a compact spec; **L19 and L20 have ratified jobs, journey roles, and acquisition postures but no gate review yet**. The gates below that named the futur-proche ownership point as the top risk are **resolved** (§10 gates 4/5/10/11). Charter status is unchanged: `L18+ | Open / provisional`, promotion pending lesson-spec completion.

- **Is L10–L20 ready for detailed spec writing?** **Partially.** **L11 is ready now.** **L12–L20 are NOT** — they hinge on unresolved decision gates (above all the futur-proche ownership point + free-tier tuning, §10; the paywall position is settled). The *arc* (Option C) is ready as a **working plan**, not a locked sequence. *(Superseded by the status update above.)*
- **Should L11–L20 be generated in one batch?** **No.** One lesson (or at most one pair) at a time, audited against the gates. Batching invites drift, overload, and paywall leakage.
- **Safest next detailed spec:** **L11 — Pouvoir-light / Help & Permission**, scoped split-sense (own `je peux`/`on peut … ?`/`vous pouvez m'aider ?`, reuse `faire ça`/`faire une pause`/`aller`; defer broad pouvoir; keep `est-ce que je peux …?` recognition).
- **Highest-priority commercial/pedagogical risk:** ~~**the futur-proche ownership point + free-tier tuning**~~ **— RESOLVED (2026-08-08).** With the preview lesson retired and FP-C capped at one recognition-only beat, the band spends **no** headline engine and has no tuning lever left. **The top remaining risks are now (a) L18 under-delivering** — one adverb authored as a frozen greeting variant rather than a productive slot (gate review §4.2) — **and (b) L20 being the first shipped Milestone**, whose contract has never been empirically tested. (The paywall position is **settled** at Campfire ~L24; a minor **docs-alignment risk** remains — stale L14 references in the repo to clean up later.) Close third, unchanged: **stacking control layers (pouvoir → question → y → devoir) too densely** — Option C mitigates this; Option B does not.

---

## Open Items / Notes

- This is **v0** — revise after L11 ships and after the §10 gates are answered (especially the futur-proche ownership point + free-tier tuning). *(L11–L17 have shipped; gates 3, 7, 8, and now 4, 5, 10, 11 are resolved. **The map is not rewritten to v1** — L18–L20 are reconciled in place and the authoritative record for them is `docs/syllabus/L18-L20-sequence-decision-v1.md`.)*
- **L19 and L20 each still need a gate review** before a compact spec; neither is written. **L18's gate review exists**: `docs/syllabus/L18-question-expansion-2-gate-review.md`.
- **`[OPEN]` RR-A — the orphaned repair rail.** `chunk-je-ne-comprends-pas` and `chunk-vous-pouvez-repeter` are PR-07 registrations no shipped lesson works; `c'est pas grave` has no identity. Acknowledged, unassigned, **non-blocking** for L18–L20, and **not** solved by L19's "weak-point repair" (which is the mastery mechanism).
- **`[OPEN]` prose archetype #3 has no `LessonArchetype` enum member.** "Negation / Question / Social Choice" is a planning label only; shipped L8 and L12 both declare `chunk-natural-speech`, and L18 follows them. Pre-existing schema gap — surfaced, not fixed.
- **Possible doc patches (propose later, not now):** (a) add a one-line **"promise-zone boundary = Campfire ~L24"** note to `docs/learning-engine-v1.md` and/or the archetype doc so future specs stop having to re-derive it; (b) a **docs-alignment pass** to mark-legacy / align the stale **"paywall after L14"** line in `CLAUDE.md` to Campfire ~L24 (surface to operator — do not edit unprompted).
- No runtime/code/content/flag/ID change is authorized by this document. Dev APK scope (L1–L5 only, no paywall) is unaffected.

*End of L10–L20 Band Map v0 (L18–L20 reconciled 2026-08-08). Planning artifact only — no lesson specs, no code/content/registry/flag/runtime change. The Dev APK smoke test remains the boundary before any runtime work. Paywall position is settled (Campfire ~L24). The §10 engine-ownership/tuning gates are now resolved: futur proche is owned post-Campfire only, its standalone preview lesson is retired, and its sole pre-Campfire exposure is FP-C at the end of L20. For anything concerning L18, L19, or L20, `docs/syllabus/L18-L20-sequence-decision-v1.md` and `docs/syllabus/L18-question-expansion-2-gate-review.md` are authoritative over this map.*
